#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strcat - concatinates two strings together.
 * @*dest: 1st input
 * @*src : 2nd input
 * Return: Always 0.
 */

char *_strcat(char *dest, char *src)
{
	*dest;
	*src;

	return (strcat(dest, src));
}

#include "main.h"

/**
 * _islower- Entry
 * Return: 1 if true, 0 otherwise
 * output- gives 1 if out is true, 0 otherwise
*/

int _islower(void);
{
	int c;

	if (c >= 97 && c <= 122)
	{
		return (1);
	}
	return (0);
}


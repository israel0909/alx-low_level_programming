#include <stdio.h>

/**
 * main- going to reverse with increment relational operator
 * Return: 0
*/

int main(void)
{
	char za;

	for (za = 'z'; za => 'a'; za--)
	{
		putchar (za);
	}
	putchar ('\n');
	return (0);
}
